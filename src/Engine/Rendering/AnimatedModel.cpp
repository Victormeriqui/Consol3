#include "AnimatedModel.hpp"

namespace Engine
{
	namespace Rendering
	{
		AnimatedModel::AnimatedModel()
		{
		}

		const VertexBuffer& AnimatedModel::GetVertexBuffer(const std::string& animation_name, float interpolation) const
		{
			// TODO
			return VertexBuffer();
		}
	}
}
